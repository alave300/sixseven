#include <iostream>
#include <vector>
using namespace std;
vector <int> c(int a){
    vector <int> o;

    o.push_back(a*1);
    o.push_back(a*2);
    o.push_back(a*3);
    
    return o;
}

int main(){
    vector<int> a = c(5);
for(int i=0; i<a.size(); i++){
    cout<<a[i];
}
}
